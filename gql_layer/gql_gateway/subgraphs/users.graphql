extend type Query {
  users: [User]
}

extend type Mutation {
  saveUser(
    id: ID!
    name: String!
    birthDate: String!
  ): String
}

type User @key(fields: "id") {
  id: ID!
  name: String
  birthDate: String
  username: String
}
